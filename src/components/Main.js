export default function Main({ children }) {
  // the main body of all the pages containing all content not present in the header of the footer

  return (
    <main>
      {children}
    </main>
  );
}
